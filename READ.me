1. To properly open the project, it is best to start a server
2. To use the tool, first run 
    startSubscription(); 
    in the browsers console, then run
    SoundnessVerifier().check(
        LoopDecomposition().decompose(
            SCC().findSCCs(
                Normalizer.normalize(
                    analyze()
                )
            )
        )
    );